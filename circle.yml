machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker -v

test:
  override:
    - docker run --rm -ti -v /run/docker.sock:/var/run/docker.sock -v `pwd`:/src -w /src golang:1.5.1-wheezy make test

